export interface DataRow {
  name: string;
  states: { name: string };
  manufacturers: { name: string };
  serial: string;
  computertypes: { name: string };
  locations: string;
  date_mod: string;
}
