export interface MonitorData {
  name: string | null,
  date_mod: string | null,
  contact: string | null,
  contact_num: string | null,
  comment: string | null,
  serial: string | null,
  otherserial: string | null,
  size: string | null,
  have_micro: number | null,
  have_speaker: number | null,
  have_subd: number | null,
  have_bnc: number | null,
  have_dvi: number | null,
  have_pivot: number | null,
  have_hdmi: number | null,
  have_displayport: number | null,
  is_deleted: number | null,
  is_global: number | null,
  is_template: number | null,
  template_name: string | null,
  ticket_tco: string | null,
  is_dynamic: number | null,
  uuid: string | null,
  date_creation: string | null,
  is_recursive: number | null,
  entities: string | null,
  users_tech: number | null,
  groups_tech: string | null,
  locations: number | null,
  monitortypes: number | null,
  monitormodels: number | null,
  manufacturers: number | null,
  users: number | null,
  groups: string | null,
  states: number | null,
  autoupdatesystems: number | null,
}