export interface EnclosureData {
  name: string | null,
  states: number | null,
  locations: number | null,
  users_tech: number | null,
  manufacturers: number | null,
  groups_tech: number | null,
  enclosuremodels: number | null,
  serial: string | null,
  otherserial: string | null,
  comment: string | null,
  power_supplies: number | null
}