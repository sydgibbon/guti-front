export interface CableData {

  name: string | null,
  is_recursive: number | null,
  itemtype_endpoint_a: string | null,
  itemtype_endpoint_b: string | null,
  color: string | null,
  otherserial: string | null,
  comment: string | null,
  date_mod: string | null,
  date_creation: string | null,
  entities: string | null,
  items_endpoint_a: number | null,
  items_endpoint_b: number | null,
  socketmodels_endpoint_a: number | null,
  socketmodels_endpoint_b: number | null,
  sockets_endpoint_a: number | null,
  sockets_endpoint_b: number | null,
  cablestrands: number | null,
  states: number | null,
  users_tech: number | null,
  cabletypes: number | null,
  
}